# Compiled binary
/soiree

# Local state and exports. Never commit real event data.
*.local.json
soiree-*.json

# Editor / OS
.DS_Store
*.swp

# Agent worktrees. Each is a full checkout, so a `git add -A` at the root would
# commit one as an embedded repository.
.claude/worktrees/
